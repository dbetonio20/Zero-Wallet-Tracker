import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zerowallet.tracker',
  appName: 'Zero Wallet Tracker',
  webDir: 'dist/zero-wallet-tracker/browser'
};

export default config;
